openapi: 3.0.3
info:
  title: Live demo API
  version: 1.1.0
paths:
  /health:
    get:
      summary: Health check
      description: Deliberately incompatible demo change.
      responses:
        "503":
          description: Service unavailable
